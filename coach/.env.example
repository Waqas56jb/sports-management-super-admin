# Backend API base URL (defaults to the production API on Railway when empty).
VITE_API_BASE_URL=https://terrific-smile-production-85ea.up.railway.app/api/v1
# "true" = use the built-in demo data instead of the API.
VITE_USE_MOCK_API=false
